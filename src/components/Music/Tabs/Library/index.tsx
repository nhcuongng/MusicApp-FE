import React from 'react';
import {Text, View} from 'react-native';

type TLibraryProps = {};

const Library = (props: TLibraryProps) => {
  return (
    <View>
      <Text>Library</Text>
    </View>
  );
};

export default Library;
